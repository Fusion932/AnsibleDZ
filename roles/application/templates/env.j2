# A key to track on which app server application was deployed
APP_KEY={{ local_app_name }}

# Port to listen on
APP_PORT={{ local_app_port }}

# Application environment, must be set to "production" when deploying
NODE_ENV={{ local_env }}
